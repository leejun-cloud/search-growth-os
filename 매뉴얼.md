# Search Growth OS — 운영 매뉴얼

고객 홈페이지를 **지속적으로 검색 유입(SEO/GEO)을 만들어내는 성장 시스템**으로 바꿔주는 멀티테넌트 엔진.
"글 쓰는 AI"가 아니라 **어떤 페이지를 왜 만들지 판단하고, 품질 통과분만 발행하고, 성과를 보고 다음 액션을 정하는** 프로그램이다.

## 설치

```bash
npm install
```

## 실행

```bash
npm run dev        # 개발 서버 (http://localhost:3000)
npm run build      # 프로덕션 빌드
npm start          # 프로덕션 서버
```

## 데이터베이스

- **기본: 설정 불필요.** 내장형 PostgreSQL(PGlite)이 `data/pglite/`에 자동 생성된다.
- **Neon 클라우드로 전환:** [neon.tech](https://neon.tech) 가입 → 프로젝트 생성 → 접속 주소 복사 → `.env.local`에 한 줄:
  ```
  DATABASE_URL=postgresql://...
  ```
  로컬과 클라우드는 100% 같은 SQL(스키마: `src/lib/sql/schema.sql` — 주석이 SQL 교재 역할).
- **SQL 직접 실행/연습:**
  ```bash
  npm run sql -- "SELECT id, name FROM sites"
  npm run sql -- "SELECT query, opportunity_score, status FROM search_opportunities ORDER BY opportunity_score DESC"
  ```
  ⚠️ PGlite는 한 번에 한 프로세스만 접속 가능 — **개발 서버가 켜져 있으면 `npm run sql`이 멈춘다.** 서버를 끄고 실행하거나 Neon으로 전환하면 동시 접속 가능.

## AI 설정

별도 API 키 불필요 — 로그인된 CLI를 그대로 사용한다.

| 환경변수 | 값 | 설명 |
|---|---|---|
| `SGO_AI` | `claude-cli` (기본) | Claude Code CLI(`claude -p`)를 호출해 생성 |
| | `codex-cli` | OpenAI Codex CLI(`codex exec`) 사용 |
| | `mock` | AI 없이 파이프라인 구조 테스트용 |

## 사용 순서 (핵심 루프)

1. **사이트 등록** — 대시보드 → `+ 사이트 등록` (이름, 도메인, 서비스, 전환 목표)
2. **진단** — 개요 탭 → `진단 실행`: 검색로봇 관점에서 크롤링해 title 중복, SSR 위험(Rendering Gate), sitemap/RSS/robots/JSON-LD 유무를 검사
3. **사업 지식 입력** — 사업 지식 탭: 서비스·가격·FAQ·프로세스 입력 (**여기 입력된 것만 글의 사실 근거가 된다**) → `Entity 추출 (AI)`
4. **검색기회 발굴** — 검색기회 탭 → `검색기회 발굴 (AI)` → `전체 채점`: 100점 만점 채점 (80+ 자동초안 / 65+ 검토 / 50+ 보강 / 미만 생성 금지)
5. **초안 생성** — 통과 후보의 `초안 생성`: Fact Pack(사실 자료집) → 본문·메타·FAQ·JSON-LD·내부링크 자동 생성 → 품질검사 자동 실행
6. **검수·발행** — 페이지 상세에서 미리보기(`🌐 실제 페이지로 보기`)·Fact Pack·품질검사 확인 → `발행`
   - 품질검사 **block**(중복/얇은 본문/지역명 치환/근거 없는 수치)이면 발행이 거부된다 — 이게 정상 동작
7. **성과 수집** — 성과 탭: Google Search Console → 실적 → 내보내기 CSV 업로드 (발행 후 노출까지 보통 수 주)
8. **Growth 분석** — Growth 탭 → `분석 실행`: UPDATE / TITLE_TEST / EXPAND / MERGE / NOINDEX / CREATE 제안 → CREATE는 클릭 한 번으로 새 검색기회 등록 → 4번으로 반복

## 발행 방식 (Site Adapter)

| 방식 | 조건 | 결과 |
|---|---|---|
| **직접 서빙** | 기본 | `/p/{siteId}/{slug}`에서 실제 SSR 웹페이지로 서빙 (JSON-LD·canonical·noindex 미리보기 포함), sitemap: `/p/{siteId}/sitemap.xml` |
| **파일 출력** | 기본 | `data/published/{siteId}/`에 md+json+sitemap.xml+rss.xml+robots.txt+llms.txt 생성 |
| **기존 사이트로 발행** | `.env.local`에 `SGO_PUBLISH_ENDPOINT`, `SGO_PUBLISH_SECRET` | 고객 사이트(Next.js 등)의 수신 API로 POST |

## 환경변수 정리 (.env.local)

```bash
DATABASE_URL=          # (선택) Neon PostgreSQL — 없으면 로컬 PGlite
SGO_AI=claude-cli      # (선택) claude-cli | codex-cli | mock
SGO_PUBLISH_ENDPOINT=  # (선택) 대상 사이트 발행 수신 URL
SGO_PUBLISH_SECRET=    # (선택) 발행 인증 시크릿
```

## 자주 쓰는 명령

```bash
SGO_AI=mock npm run demo   # 전체 파이프라인 관통 테스트 (AI 없이)
npm run demo               # 실제 Claude CLI로 데모 (생성 품질 확인)
npm run sql -- "..."       # SQL 직접 실행
```

## 운영 팁 / 주의

- **파일럿 판정 기준을 지켜라**: 발행 30~50페이지, 8주 내 노출 쿼리 50개 기준(사이트 설정에서 조정). 미달이면 확장을 멈추고 원인 분석 — 성과탭이 자동 판정한다.
- **지역명만 바꾼 페이지는 시스템이 차단한다** (Template Family 유사도 45% 이상 block). 차단되면 각 지역의 고유 데이터(병원 정보·정책·가격 규칙)를 Entity에 추가하고 재생성하라.
- **네이버**: 웹사이트 SEO만으로는 네이버 상단 진입이 제한적(블로그/플레이스 우선 노출). 네이버 유입이 중요하면 스마트플레이스·네이버 블로그 병행 필요.
- 발행 → 색인 → 노출 데이터까지 **2~6개월**이 정상. 조기 판단 금지.
- 개발 서버 첫 접속 시 페이지별 컴파일로 20~30초 걸릴 수 있다 (한 번 컴파일되면 빠름).

## 기획 문서 (개발 전 필독)

`PRD.md` (제품 기획) → `DECISIONS.md` (확정된 설계 결정) → `REFERENCES.md` (참고 레포/공식 가이드)
